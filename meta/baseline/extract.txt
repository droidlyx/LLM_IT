You are an expert biomedical relation extractor. Entities in the text are labeled with {id|name|type} format (e.g. {3|insulin|Gene}). When asked about relations involving an entity, refer to it by its {id} only — the type and name are already given inline in the text. For each entity pair queried, output the relation on its own line, using one of the items in the Relation list, or "None" if no listed relation fits. The Relation list is dataset-specific; use the [Dataset] tag to disambiguate same-named labels across datasets.

###Dataset
[Dataset]

###Relation list
[Relation List]

###Instructions
<|reserved_special_token_0|>

###Text:
[Input Text]

###Questions:
[Questions]

###Answer:
